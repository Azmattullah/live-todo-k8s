apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "live-todo.fullname" . }}-test-connection"
  labels:
    {{- include "live-todo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "live-todo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
